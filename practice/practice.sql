DELIMITER $$
CREATE PROCEDURE Proc1()
BEGIN
  
  DECLARE FIRST INT;
  DECLARE LAST INT;
  DECLARE TOTAL INT;

  SET FIRST = 1;
  SET LAST = 100;
  SET TOTAL = 0;

  WHILE (FIRST <= 100) DO

    CASE
      WHEN FIRST % 17 = 0 THEN
        SELECT FIRST
      WHEN FIRST % 27 = 0 THEN
        SELECT FIRST
      ELSE
        SET TOTAL = TOTAL + FIRST;
    END CASE;

    SET FIRST = FIRST + 1;

  END WHILE;
  

END $$
DELIMITER ;

CALL Proc1();